---
title: "Add `withNoIncrementalHydration()` to `provideClientHydration()`"
sidebar_label: "Add `withNoIncrementalHydration()` to `provideClientHydration()`"
hide_title: true
---

import { RecipeHeader, RecipeMeta, RecipeList, OptionsTable, ExampleList, UsageList, DataTableList } from '@site/src/components/recipe';

<RecipeMeta
  displayName={"Add `withNoIncrementalHydration()` to `provideClientHydration()`"}
  description={"Adds the `withNoIncrementalHydration()` opt out to `provideClientHydration()` calls that do not already configure incremental hydration. Angular 22 enables incremental hydration by default; the opt out retains the previous behavior."}
  fqName={"org.openrewrite.angular.migration.add-no-incremental-hydration"}
  languages={["OpenRewrite"]}
  license={"Moderne Proprietary License"}
/>

<RecipeHeader
  type={"Single recipe"}
  languages={["OpenRewrite"]}
  tags={[]}
  license={"Moderne Proprietary License"}
  fqName={"org.openrewrite.angular.migration.add-no-incremental-hydration"}
  artifact={"@openrewrite/recipes-angular"}
  appLink={"https://app.moderne.io/recipes/org.openrewrite.angular.migration.add-no-incremental-hydration"}
  markdownUrl={"https://raw.githubusercontent.com/moderneinc/moderne-docs/refs/heads/main/docs/user-documentation/recipes/recipe-catalog/angular/migration/add-no-incremental-hydration.md"}
  moderneOnly
>

<RecipeHeader.Title>Add `withNoIncrementalHydration()` to `provideClientHydration()`</RecipeHeader.Title>

<RecipeHeader.Description>Adds the `withNoIncrementalHydration()` opt out to `provideClientHydration()` calls that do not already configure incremental hydration. Angular 22 enables incremental hydration by default; the opt out retains the previous behavior.</RecipeHeader.Description>

</RecipeHeader>

<UsageList usage={{"recipeName":"org.openrewrite.angular.migration.add-no-incremental-hydration","displayName":"Add `withNoIncrementalHydration()` to `provideClientHydration()`","npmPackage":"@openrewrite/recipes-angular"}}>

## Usage

</UsageList>

