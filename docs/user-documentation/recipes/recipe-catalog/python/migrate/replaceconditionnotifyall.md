---
title: "Replace `Condition.notifyAll()` with `Condition.notify_all()`"
sidebar_label: "Replace `Condition.notifyAll()` with `Condition.notify_all()`"
hide_title: true
---

import { RecipeHeader, RecipeMeta, RecipeList, OptionsTable, ExampleList, UsageList, DataTableList } from '@site/src/components/recipe';

<RecipeMeta
  displayName={"Replace `Condition.notifyAll()` with `Condition.notify_all()`"}
  description={"Replace `notifyAll()` method calls with `notify_all()`. The camelCase version was deprecated in Python 3.10 and removed in 3.12."}
  fqName={"org.openrewrite.python.migrate.ReplaceConditionNotifyAll"}
  languages={["Python"]}
  license={"Moderne Proprietary License"}
/>

<RecipeHeader
  type={"Single recipe"}
  languages={["Python"]}
  tags={["python","migration","threading","3.12","3.10"]}
  license={"Moderne Proprietary License"}
  fqName={"org.openrewrite.python.migrate.ReplaceConditionNotifyAll"}
  artifact={"openrewrite-migrate-python"}
  appLink={"https://app.moderne.io/recipes/org.openrewrite.python.migrate.ReplaceConditionNotifyAll"}
  markdownUrl={"https://raw.githubusercontent.com/moderneinc/moderne-docs/refs/heads/main/docs/user-documentation/recipes/recipe-catalog/python/migrate/replaceconditionnotifyall.md"}
  moderneOnly
>

<RecipeHeader.Title>Replace `Condition.notifyAll()` with `Condition.notify_all()`</RecipeHeader.Title>

<RecipeHeader.Description>Replace `notifyAll()` method calls with `notify_all()`. The camelCase version was deprecated in Python 3.10 and removed in 3.12.</RecipeHeader.Description>

</RecipeHeader>

<UsageList usage={{"recipeName":"org.openrewrite.python.migrate.ReplaceConditionNotifyAll","displayName":"Replace `Condition.notifyAll()` with `Condition.notify_all()`","pipPackage":"openrewrite-migrate-python","versionKey":"VERSION_ORG_OPENREWRITE_RECIPE_REWRITE_MIGRATE_PYTHON"}}>

## Usage

</UsageList>

