---
title: "Use `()` literal instead of `tuple()` constructor"
sidebar_label: "Use `()` literal instead of `tuple()` constructor"
hide_title: true
---

import { RecipeHeader, RecipeMeta, RecipeList, OptionsTable, ExampleList, UsageList, DataTableList } from '@site/src/components/recipe';

<RecipeMeta
  displayName={"Use `()` literal instead of `tuple()` constructor"}
  description={"Convert no-argument `tuple()` calls to the `()` literal, which is more concise and avoids a function call."}
  fqName={"org.openrewrite.python.cleanup.TupleLiteral"}
  languages={["Python"]}
  license={"Moderne Proprietary License"}
/>

<RecipeHeader
  displayName={"Use `()` literal instead of `tuple()` constructor"}
  description={"Convert no-argument `tuple()` calls to the `()` literal, which is more concise and avoids a function call."}
  type={"Single recipe"}
  languages={["Python"]}
  tags={[]}
  license={"Moderne Proprietary License"}
  fqName={"org.openrewrite.python.cleanup.TupleLiteral"}
  artifact={"org.openrewrite.recipe:rewrite-static-analysis-python"}
  appLink={"https://app.moderne.io/recipes/org.openrewrite.python.cleanup.TupleLiteral"}
  markdownUrl={"https://raw.githubusercontent.com/moderneinc/moderne-docs/refs/heads/main/docs/user-documentation/recipes/recipe-catalog/python/cleanup/tupleliteral.md"}
  moderneOnly
/>

<UsageList usage={{"recipeName":"org.openrewrite.python.cleanup.TupleLiteral","displayName":"Use `()` literal instead of `tuple()` constructor","pipPackage":"openrewrite-static-analysis"}}>

## Usage

</UsageList>

