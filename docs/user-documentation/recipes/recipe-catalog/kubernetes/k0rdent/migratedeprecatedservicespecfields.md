---
title: "Migrate deprecated k0rdent `serviceSpec` fields"
sidebar_label: "Migrate deprecated k0rdent `serviceSpec` fields"
hide_title: true
---

import { RecipeHeader, RecipeMeta, RecipeList, OptionsTable, ExampleList, UsageList, DataTableList } from '@site/src/components/recipe';

<RecipeMeta
  displayName={"Migrate deprecated k0rdent `serviceSpec` fields"}
  description={"Move the fields k0rdent deprecated on `spec.serviceSpec` into the `spec.serviceSpec.provider.config` blob that supersedes them, on `ClusterDeployment` and `MultiClusterService`. The nine that move are `syncMode`, `templateResourceRefs`, `policyRefs`, `driftIgnore`, `driftExclusions`, `priority`, `stopOnConflict`, `reload` and `continueOnError`, plus `createNamespace` and `replace` on each service's `helmOptions`, which move into `helmOptions.installOptions`.\n\n`reload` is renamed to `reloader` on the way, because that is the key the state management provider unmarshals; a hand migration that keeps the old spelling produces a manifest the apiserver accepts and the controller ignores.\n\nFields are left where they are, and reported in a data table, when the move would change what the resource does: the document renders through Helm, so what is written is not what is applied; `provider.name` or `provider.config` is already set, in which case the controller is already ignoring the deprecated fields and moving them would switch settings on; `installOptions` already declares a different value; or the resource is declared against `k0rdent.mirantis.com/v1alpha1`, whose schema has no `provider` field at all, so the apiserver would prune whatever was written there.\n\nThe `provider.name` case is the subtle one. `StateManagementProviderConfigFromServiceSpec` folds the deprecated fields into a config blob only when neither `provider.name` nor `provider.config` is set; name it, and every deprecated field is discarded. Moving them into `provider.config` would hand a named provider a `syncMode`, `priority` or `reloader` it is not acting on today."}
  fqName={"org.openrewrite.kubernetes.k0rdent.MigrateDeprecatedServiceSpecFields"}
  languages={["OpenRewrite"]}
  license={"Moderne Proprietary License"}
/>

<RecipeHeader
  type={"Single recipe"}
  languages={["OpenRewrite"]}
  tags={[]}
  license={"Moderne Proprietary License"}
  fqName={"org.openrewrite.kubernetes.k0rdent.MigrateDeprecatedServiceSpecFields"}
  artifact={"org.openrewrite.recipe:rewrite-kubernetes"}
  appLink={"https://app.moderne.io/recipes/org.openrewrite.kubernetes.k0rdent.MigrateDeprecatedServiceSpecFields"}
  markdownUrl={"https://raw.githubusercontent.com/moderneinc/moderne-docs/refs/heads/main/docs/user-documentation/recipes/recipe-catalog/kubernetes/k0rdent/migratedeprecatedservicespecfields.md"}
  moderneOnly
>

<RecipeHeader.Title>Migrate deprecated k0rdent `serviceSpec` fields</RecipeHeader.Title>

<RecipeHeader.Description>Move the fields k0rdent deprecated on `spec.serviceSpec` into the `spec.serviceSpec.provider.config` blob that supersedes them, on `ClusterDeployment` and `MultiClusterService`. The nine that move are `syncMode`, `templateResourceRefs`, `policyRefs`, `driftIgnore`, `driftExclusions`, `priority`, `stopOnConflict`, `reload` and `continueOnError`, plus `createNamespace` and `replace` on each service's `helmOptions`, which move into `helmOptions.installOptions`.  `reload` is renamed to `reloader` on the way, because that is the key the state management provider unmarshals; a hand migration that keeps the old spelling produces a manifest the apiserver accepts and the controller ignores.  Fields are left where they are, and reported in a data table, when the move would change what the resource does: the document renders through Helm, so what is written is not what is applied; `provider.name` or `provider.config` is already set, in which case the controller is already ignoring the deprecated fields and moving them would switch settings on; `installOptions` already declares a different value; or the resource is declared against `k0rdent.mirantis.com/v1alpha1`, whose schema has no `provider` field at all, so the apiserver would prune whatever was written there.  The `provider.name` case is the subtle one. `StateManagementProviderConfigFromServiceSpec` folds the deprecated fields into a config blob only when neither `provider.name` nor `provider.config` is set; name it, and every deprecated field is discarded. Moving them into `provider.config` would hand a named provider a `syncMode`, `priority` or `reloader` it is not acting on today.</RecipeHeader.Description>

</RecipeHeader>

<OptionsTable options={[{"type":"String","name":"fileMatcher","required":false,"description":"Matching files will be modified. This is a glob expression.","example":"**/*-multiclusterservice.yaml"}]}>

## Options

</OptionsTable>

<ExampleList examples={[{"parameters":[{"parameter":"fileMatcher","value":"null"}],"variants":[{"language":"yaml","before":"apiVersion: k0rdent.mirantis.com/v1beta1\nkind: MultiClusterService\nmetadata:\n  name: global-kyverno\nspec:\n  clusterSelector:\n    matchLabels:\n      k0rdent: demo\n  serviceSpec:\n    syncMode: ContinuousWithDriftDetection\n    priority: 1000\n    reload: true\n    templateResourceRefs:\n      - identifier: ConfigMap\n        resource:\n          apiVersion: v1\n          kind: ConfigMap\n          name: kof-namespace-template\n    services:\n      - template: kyverno-3-2-6\n        name: kyverno\n        namespace: kyverno\n---\napiVersion: k0rdent.mirantis.com/v1beta1\nkind: ClusterDeployment\nmetadata:\n  name: untouched\nspec:\n  template: aws-standalone-cp-1-0-42\n  serviceSpec:\n    services:\n      - template: ingress-nginx-4-11-0\n        name: ingress-nginx\n        namespace: ingress-nginx\n","after":"apiVersion: k0rdent.mirantis.com/v1beta1\nkind: MultiClusterService\nmetadata:\n  name: global-kyverno\nspec:\n  clusterSelector:\n    matchLabels:\n      k0rdent: demo\n  serviceSpec:\n    services:\n      - template: kyverno-3-2-6\n        name: kyverno\n        namespace: kyverno\n    provider:\n      config:\n        syncMode: ContinuousWithDriftDetection\n        priority: 1000\n        reloader: true\n        templateResourceRefs:\n          - identifier: ConfigMap\n            resource:\n              apiVersion: v1\n              kind: ConfigMap\n              name: kof-namespace-template\n---\napiVersion: k0rdent.mirantis.com/v1beta1\nkind: ClusterDeployment\nmetadata:\n  name: untouched\nspec:\n  template: aws-standalone-cp-1-0-42\n  serviceSpec:\n    services:\n      - template: ingress-nginx-4-11-0\n        name: ingress-nginx\n        namespace: ingress-nginx\n","diff":"--- MultiClusterServices/kyverno.yaml\n+++ MultiClusterServices/kyverno.yaml\n@@ -10,9 +10,0 @@\n      k0rdent: demo\n  serviceSpec:\n-   syncMode: ContinuousWithDriftDetection\n-   priority: 1000\n-   reload: true\n-   templateResourceRefs:\n-     - identifier: ConfigMap\n-       resource:\n-         apiVersion: v1\n-         kind: ConfigMap\n-         name: kof-namespace-template\n    services:\n@@ -23,0 +14,11 @@\n        name: kyverno\n        namespace: kyverno\n+   provider:\n+     config:\n+       syncMode: ContinuousWithDriftDetection\n+       priority: 1000\n+       reloader: true\n+       templateResourceRefs:\n+         - identifier: ConfigMap\n+           resource:\n+             apiVersion: v1\n+             kind: ConfigMap\n+             name: kof-namespace-template\n---\n","newFile":false}]}]}>

## Examples

</ExampleList>

<UsageList usage={{"recipeName":"org.openrewrite.kubernetes.k0rdent.MigrateDeprecatedServiceSpecFields","displayName":"Migrate deprecated k0rdent `serviceSpec` fields","groupId":"org.openrewrite.recipe","artifactId":"rewrite-kubernetes","versionKey":"VERSION_ORG_OPENREWRITE_RECIPE_REWRITE_KUBERNETES","requiresConfiguration":false}}>

## Usage

</UsageList>

<DataTableList tables={[{"name":"org.openrewrite.kubernetes.table.DeprecatedServiceSpecFields","displayName":"Deprecated k0rdent `serviceSpec` fields","description":"Every deprecated `serviceSpec` field found, where it was moved to, and for the ones that were left in place, why.","columns":[{"name":"Source path","description":"The path of the manifest the field was found in."},{"name":"Kind","description":"The `kind` of the resource carrying the field."},{"name":"Name","description":"The `metadata.name` of the resource carrying the field."},{"name":"Field","description":"The deprecated field, relative to the document root."},{"name":"Moved to","description":"Where the field was written, empty when it was left in place."},{"name":"Outcome","description":"`Moved` when the field was relocated, `Renamed` when it was relocated under a different name, `Templated` when the document renders through Helm and so does not mean on disk what it means in the cluster, `Provider config already set` when `provider.config` is present and the controller is therefore already ignoring this field, `Provider already named` when `provider.name` is set, which discards the deprecated fields just as surely, `Conflicting value` when the destination already declares something else, `Unsupported API version` when the resource is declared against a version whose schema has no destination for it, or `Unsupported layout` when the field or its destination is written as a flow mapping, which entries can be neither lifted out of nor appended to, or holds a block scalar, whose continuation lines cannot be re-indented with it."}]},{"name":"org.openrewrite.table.SourcesFileResults","displayName":"Source files that had results","description":"Source files that were modified by the recipe run.","columns":[{"name":"Source path before the run","description":"The source path of the file before the run. `null` when a source file was created during the run."},{"name":"Source path after the run","description":"A recipe may modify the source path. This is the path after the run. `null` when a source file was deleted during the run."},{"name":"Parent of the recipe that made changes","description":"In a hierarchical recipe, the parent of the recipe that made a change. Empty if this is the root of a hierarchy or if the recipe is not hierarchical at all."},{"name":"Recipe that made changes","description":"The specific recipe that made a change."},{"name":"Estimated time saving","description":"An estimated effort that a developer to fix manually instead of using this recipe, in unit of seconds."},{"name":"Cycle","description":"The recipe cycle in which the change was made."}]},{"name":"org.openrewrite.table.SearchResults","displayName":"Source files that had search results","description":"Search results that were found during the recipe run.","columns":[{"name":"Source path of search result before the run","description":"The source path of the file with the search result markers present."},{"name":"Source path of search result after run the run","description":"A recipe may modify the source path. This is the path after the run. `null` when a source file was deleted during the run."},{"name":"Result","description":"The trimmed printed tree of the LST element that the marker is attached to."},{"name":"Description","description":"The content of the description of the marker."},{"name":"Recipe that added the search marker","description":"The specific recipe that added the Search marker."}]},{"name":"org.openrewrite.table.SourcesFileErrors","displayName":"Source files that errored on a recipe","description":"The details of all errors produced by a recipe run.","columns":[{"name":"Source path","description":"The file that failed to parse."},{"name":"Recipe that made changes","description":"The specific recipe that made a change."},{"name":"Stack trace","description":"The stack trace of the failure."}]},{"name":"org.openrewrite.table.RecipeRunStats","displayName":"Recipe performance","description":"Statistics used in analyzing the performance of recipes.","columns":[{"name":"The recipe","description":"The recipe whose stats are being measured both individually and cumulatively."},{"name":"Source file count","description":"The number of source files the recipe ran over."},{"name":"Source file changed count","description":"The number of source files which were changed in the recipe run. Includes files created, deleted, and edited."},{"name":"Cumulative scanning time (ns)","description":"The total time spent across the scanning phase of this recipe."},{"name":"Max scanning time (ns)","description":"The max time scanning any one source file."},{"name":"Cumulative edit time (ns)","description":"The total time spent across the editing phase of this recipe."},{"name":"Max edit time (ns)","description":"The max time editing any one source file."}]}]}>

## Data tables

</DataTableList>

